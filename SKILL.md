---
name: model-vision
description: "配合 cc-switch 使用的图片处理技能。自动拦截图片请求，分流到视觉模型分析，再将文字结果注入主模型推理。"
user-invocable: true
argument-hint: <图片路径> [问题]
---

# model-vision

配合 [cc-switch](https://github.com/nicepkg/cc-switch) 使用的图片处理技能。

主模型保持不变；本技能只负责在需要看图时调用视觉模型做图片理解，然后把文字结果注入主模型继续推理。视觉模型可通过 `router/config.json` 的 `model_image` 字段任意替换。

## 什么时候用

出现以下情况时，必须先走本技能：

- 用户给了本地图片路径
- 用户要求分析截图、照片、图表、界面图
- 需要 OCR、提取图中文字、识别表格、识别界面元素
- 需要核对图片内容和文字描述是否一致
- 推理过程中发现必须先看图，才能继续回答

## 使用方式

### 自动模式（推荐）

代理运行时，聊天中直接粘贴、拖拽图片即可，代理自动处理。

### 手动模式（本地文件）

```bash
python ~/.claude/skills/model-vision/cli/vision.py "<图片路径>" "[问题]"
```

## 上下文感知

代理会自动从对话中提取用户最近的发言，生成上下文感知的提示词：

- 有用户文本 → 视觉模型收到用户的原话，重点分析与用户意图相关的图片内容
- 无用户文本 → 使用默认提示词，全面描述图片

## 强制规则

- 不要让主模型直接处理图片内容
- 不要根据文件名、路径名、上下文猜图片里有什么
- 只要图片内容会影响最终回答，就先看图，再回答
- 聊天里直接附图走代理，本地文件图片走 `cli/vision.py`

## 配置

编辑 `router/config.json` 可替换视觉模型、调整提示词等。详见 [README.md](README.md)。
